#include <iostream>
#include <fstream>
#include <sstream>
#include <cstdlib>

#include <stdio.h>
#include <ctype.h>

using namespace std;

    string switch_words(string value);
    string bold();
    string bold_end();
    string int_to_str(int n);
    string get_color(string color_input);
    void get_logo();